apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - datasciencecluster.yaml

# Add sync wave annotation to ensure DataScienceCluster is applied after operator is ready
commonAnnotations:
  argocd.argoproj.io/sync-wave: "2"
